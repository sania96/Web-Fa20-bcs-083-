<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Recipe Form</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Recipe Form</h1>
  <form method="POST" <% if (recipe) { %>action="/recipes/<%= recipe._id %>?_method=PUT"<% } else { %>action="/recipes"<% } %>>
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= recipe ? recipe.title : '' %>" required><br>
    <label for="description">Description:</label>
    <textarea id="description" name="description" required><%= recipe ? recipe.description : '' %></textarea><br>
    <button type="submit">Save</button>
  </form>
</body>
</html>
